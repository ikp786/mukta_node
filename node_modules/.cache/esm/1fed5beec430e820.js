let CustomErrorHandler,JwtService;_c43‍.x([["default",()=>_c43‍.o]]);_c43‍.w("../services/CustomErrirHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_c43‍.w("../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);


const adminAuth = async (req,res,next) => {
  
    const {cookies} = req;
    const authHeader = cookies.admin_auth_token;

    if(!authHeader){
        return res.redirect('/admin/login');
        // return next(CustomErrorHandler.unAuthorized())
    }    
    const token = authHeader;
    try {
        const {_id,role} = await JwtService.verify(token);
        const user = {
            _id,
            role
        }
        req.user = user;
        next();
    } catch (err) {        
        // return next(CustomErrorHandler.unAuthorized())        
        return res.redirect('/admin/login');

    }
}
_c43‍.d(adminAuth); 