let Joi,bcrypt,RefreshToken,User,CustomErrorHandler,JwtService,REFRESH_SECRET;_e3c‍.x([["default",()=>_e3c‍.o]]);_e3c‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_e3c‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_e3c‍.w("../../../models",[["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}],["User",["User"],function(v){User=v}]]);_e3c‍.w("../../../services/CustomErrirHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_e3c‍.w("../../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_e3c‍.w("../../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);






const RegisterController = {
  async register(req, res, next) {
    // validation
    const pattern =
      "/(?=.*[a-z])(?=.*[A-Z])(?=.*d)(?=.*[$@$!#.])[A-Za-zd$@$!%*?&.]{8,20}/";

    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      // password: Joi.string().regex(RegExp(pattern)), // you have to put it in this way and it will work :).required().min(8).max(20)
      // reapetpassword:Joi.ref(password),
      password: Joi.string().required(),
      reapetpassword: Joi.string().required().valid(Joi.ref("password")),
    });

    const { error } = registerSchema.validate(req.body);

    if (error) {
      return next(error);
    }

    // CHEK EMAIL ALRADY EXISTS
    try {
      const exist = await User.exists({ email: req.body.email });
      if (exist) {
        return next(
          CustomErrorHandler.alreadyExist("This email is already taken.")
        );
      }
    } catch (err) {
      return next(err);
    }
    const { name, email, password } = req.body;
    // hash password
    const hashPassword = await bcrypt.hash(password, 10);

    // prepare model
    
    const user = new User ({
      name,
      email,
      password: hashPassword,
      role:'Admin'
    });
    let access_token;
    let refresh_token;
    try {
      const result = await user.save();      
      //  CREATE JWT TOKEN
      access_token = JwtService.sign({ _id: result._id, role: result.role });
      refresh_token = JwtService.sign({ _id: result._id, role: result.role },'1y',REFRESH_SECRET);
      // database whitelist
      await RefreshToken.create({token: refresh_token});

    } catch (err) {
      return next(err);
    }

    res.json({ access_token,refresh_token });
  },
};

_e3c‍.d(RegisterController);
