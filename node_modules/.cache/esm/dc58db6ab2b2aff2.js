let Joi,RefreshToken,User,bcrypt,CustomErrorHandler,JwtService,REFRESH_SECRET;_b2b‍.x([["default",()=>_b2b‍.o]]);_b2b‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_b2b‍.w("../../../models",[["RefreshToken",["RefreshToken"],function(v){RefreshToken=v}],["User",["User"],function(v){User=v}]]);_b2b‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_b2b‍.w("../../../services/CustomErrirHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_b2b‍.w("../../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);_b2b‍.w("../../../config",[["REFRESH_SECRET",["REFRESH_SECRET"],function(v){REFRESH_SECRET=v}]]);







const loginController = {

    async loginView(req,res,next){    
        res.render('admin/login')    
    },


    async dashboard(req,res,next){    
      res.render('admin/dashboard')    
  },


    async login(req,res,next){
        const loginSchema = Joi.object({
      email: Joi.string().email().required(),      
      password: Joi.string().required(),
        });
    const { error } = loginSchema.validate(req.body);
    if (error) {
        return next(error);
      }

      try {
        const user = await User.findOne({email:req.body.email});
        if(!user){
            return next(CustomErrorHandler.wrongCredentials());
        }
        const match = await bcrypt.compare(req.body.password,user.password);
        if(!match){
            return next(CustomErrorHandler.wrongCredentials());
        }
        // Token
      const access_token = JwtService.sign({ _id: user._id, role: user.role });

      const refresh_token = JwtService.sign({ _id: user._id, role: user.role },'1y',REFRESH_SECRET);

      // database whitelist
      await RefreshToken.create({token: refresh_token});
      res.cookie('admin_auth_token',access_token);
      req.flash('success', 'This is a flash message using the express-flash module.');

      return res.redirect('/admin/dashboard');

      res.json({access_token,refresh_token})

      } catch (err) {
        return next(err);
      }

    },

    async logout(req,res,next){

      // validation
 
    const refreshSchema = Joi.object({
      refresh_token: Joi.string().required(),      
  
    });
const { error } = refreshSchema.validate(req.body);
if (error) {
    return next(error);
  }


      try {
        await RefreshToken.deleteOne({ token: req.body.refresh_token });
      } catch (err) {
        return next( Error('Something went wrong in the database'));
      }
      return res.redirect('/admin/login');

      

    }

};

_b2b‍.d(loginController);